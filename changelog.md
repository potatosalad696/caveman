# Changelog
## 1.0.0
### Iteration 2
**Changes**
- Comments now use `uhh` instead of `>>`

**Fixes**
- Fixed some problems with indentation
- Printing a `yesno` now returns `yes` or `no` instead of True or False

**Technical Changes**
- `get_future()` is now used for loops, functions, and conditionals
### Iteration 1
- Added everything